zookeeper.connect=${zookeepers}/${cluster_id}
broker.id=${_ip_id}
listeners=PLAINTEXT://0.0.0.0:${_id}
advertised.listeners=PLAINTEXT://${_ip}:${_id}
log.dir=${_id_log}
offsets.topic.replication.factor=1
